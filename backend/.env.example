# Copy this to .env and fill in your keys

# OpenWeatherMap free API key — get one at https://openweathermap.org/api
# If left empty, the app uses deterministic synthetic weather data.
OPENWEATHER_API_KEY=

# CORS origin for the frontend (change in production)
FRONTEND_URL=http://localhost:5173
