# Weather is fetched from Open-Meteo (free, no API key required).
# If the request fails the app falls back to deterministic synthetic data.

# CORS origin for the frontend (change in production)
FRONTEND_URL=http://localhost:5173
